<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="hugues.marchal.cheaty.Activities.BluetoothScanActivity">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btListView"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_above="@+id/btSaveButton" />

    <Button
        android:layout_width="300dp"
        android:layout_height="50dp"
        android:text="Scan"
        android:id="@+id/btScanButton"
        android:layout_centerHorizontal="true"
        android:textSize="28sp"
        android:layout_alignParentBottom="true"
        android:textAlignment="center" />

    <Button
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:text="Save"
        android:id="@+id/btSaveButton"
        android:layout_above="@+id/btScanButton"
        android:layout_centerHorizontal="true"
        android:textSize="18sp"
        android:onClick="saveBluetoothDevice"
        android:textAlignment="center" />
</RelativeLayout>
